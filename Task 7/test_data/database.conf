# Database Configuration
host=localhost
port=5432
database=webapp_prod
username=webapp_user
password=secure_password_123

# Connection Pool Settings
max_connections=100
min_connections=10
connection_timeout=30

# Logging Configuration
log_level=INFO
log_file=/var/log/webapp/database.log
log_rotation=daily

# Backup Settings
backup_enabled=true
backup_schedule=0 2 * * *
backup_retention_days=30